---
import PageShell from "../../components/PageShell.astro";
import { getCollection } from "astro:content";
import analyticsData from "../../data/toolkit-analytics.json";

const HIDDEN_CATEGORIES = new Set(["Business Plans", "TBA", "Financial Models"]);

const all = await getCollection("toolkit");

// DEBUG
const dbTitles = Array.from(new Set(all.map((i) => i.data.dbTitle || ""))).sort();
console.log("toolkit total:", all.length);
console.log("dbTitles:", dbTitles);

const items = all
  .filter((item) => (item.data.dbTitle || "").trim() === "Frameworks")
  .filter((item) => !HIDDEN_CATEGORIES.has((item.data.primaryCategory || "Uncategorized").trim()));

console.log("frameworks visible:", items.length);

// Build grouped structure
const groupsMap = new Map();
for (const item of items) {
  const c = (item.data.primaryCategory || "Uncategorized").trim();
  if (!groupsMap.has(c)) groupsMap.set(c, []);
  groupsMap.get(c).push(item);
}

const categories = Array.from(groupsMap.keys()).sort((a, b) => a.localeCompare(b));
for (const c of categories) {
  groupsMap.get(c).sort((a, b) => a.data.title.localeCompare(b.data.title));
}
const groups = categories.map((c) => ({ category: c, items: groupsMap.get(c) }));

function catId(name) {
  return (
    "cat-" +
    String(name || "")
      .trim()
      .toLowerCase()
      .replace(/['"]/g, "")
      .replace(/[^a-z0-9]+/g, "-")
      .replace(/^-+|-+$/g, "")
  );
}

// Featured (Notion checkbox -> frontmatter `featured: true`)
const featured = items.filter((i) => i.data.featured === true).slice(0, 8);

const getFrameworkName = (item) => item?.data?.title || "—";

const analyticsCards = [
  {
    title: "Most Popular Framework",
    frameworkName: analyticsData.mostPopular.frameworkName,
    metricLabel: "Views",
    metricValue: analyticsData.mostPopular.metricValue,
  },
  {
    title: "Most Downloaded Framework",
    frameworkName: analyticsData.mostDownloaded.frameworkName,
    metricLabel: "Downloads",
    metricValue: analyticsData.mostDownloaded.metricValue,
  },
  {
    title: "Most Liked Framework",
    frameworkName: analyticsData.mostLiked.frameworkName,
    metricLabel: "Likes",
    metricValue: analyticsData.mostLiked.metricValue,
  },
];

const pmtoolkitBreadcrumbs = [
  { name: "Home", path: "/" },
  { name: "PM Toolkit", path: "/toolkit/" },
];
---

<PageShell
  title="Product Manager Toolkit"
  description="Frameworks that help product managers make better decisions."
  keywords="product manager toolkit, product management frameworks, PM frameworks, product strategy, prioritization, Altareen"
breadcrumbs={pmtoolkitBreadcrumbs}
>

<div class="toolkit" data-view="grid">
  <div class="controlsBar" role="search" aria-label="Search and filter frameworks">
    <div class="field">
      <span class="icon" aria-hidden="true">⌕</span>
      <input id="q" class="input" type="search" placeholder="Search frameworks…" />
    </div>

    <select id="cat" class="select" aria-label="Filter by category">
      <option value="">All categories</option>
      {categories.map((c) => <option value={c}>{c}</option>)}
    </select>

    <div class="rightActions">
      <div class="resultsInline" id="resultsText">Showing {items.length} frameworks</div>

      <div class="viewToggle" role="group" aria-label="View toggle">
        <button id="viewGrid" class="toggleBtn isActive" type="button" aria-pressed="true">
          Grid
        </button>
        <button id="viewCompact" class="toggleBtn" type="button" aria-pressed="false">
          Compact
        </button>
      </div>

      <button id="clear" class="ghost" type="button">Clear</button>
    </div>
  </div> 

    {analyticsCards.length > 0 && (
      <section class="analyticsStrip" aria-label="Framework performance overview">
        <div class="analyticsGrid">
          {analyticsCards.map((card) => (
            <article class="analyticsCard">
              <div class="analyticsTitle">{card.title}</div>
              <div class="analyticsFrameworkName">{card.frameworkName}</div>

              <div class="analyticsEventRow">
                <span class="analyticsEventLabel">{card.metricLabel}</span>
                <span class="analyticsEventValue">{card.metricValue}</span>
              </div>
            </article>
          ))}
        </div>
      </section>
    )}

           <!-- Featured -->
    {featured.length > 0 && (
      <section class="featuredBlock" id="__featured" data-group="__featured" data-expanded="false">
        <div class="groupHead">
          <div class="groupTitleWrap">
            <h2 class="groupTitle">
              Featured <span class="count">({featured.length})</span>
            </h2>
            <div class="groupRule" aria-hidden="true"></div>
          </div>
        </div>

        <div class="groupReveal" data-expand-wrap>
          <div class="grid" data-expand-grid>
            {featured.map((item) => (
            <a
  class="card"
  href={`/toolkit/${item.slug}/`}
  data-title={item.data.title}
  data-category={item.data.primaryCategory || "Uncategorized"}
  data-when={item.data.whenToUse || ""}
  data-featured="true"
  onclick={`trackEvent('framework_click', { framework: '${item.slug}', category: '${(item.data.primaryCategory || "Uncategorized").replace(/'/g, "\\'")}', featured: true })`}
>
                {item.data.cover ? (
                  <div class="thumb" aria-hidden="true">
                    <img class="cover" src={item.data.cover} alt={`Cover for ${item.data.title}`} loading="lazy" />
                  </div>
                ) : (
                  <div class="thumb thumbEmpty" aria-hidden="true">
                    <span class="thumbMark">AL</span>
                  </div>
                )}

                <div class="meta">
                  <div class="cardTitle">{item.data.title}</div>

                  <div class="excerpt">
                    <div class="excerptLabel">When to use</div>
                    <div class="whenToUse">
                      {item.data.whenToUse ? item.data.whenToUse : <span class="muted">—</span>}
                    </div>
                  </div>
                </div>
              </a>
            ))}
          </div>
        </div>

<div class="groupFooter">
  <button
    class="groupToggle"
    type="button"
    data-expand-toggle
    hidden
    aria-expanded="false"
    aria-label="View all frameworks in this section"
  >
<span class="groupTogglePill" aria-hidden="true">
  <img class="groupToggleIcon" src="/homepage/downarrow.png" alt="" />
</span>
  </button>
</div>
      </section>
    )}

    <!-- Empty state -->
    <div id="empty" class="empty" hidden>
      <div class="emptyTitle">No frameworks found</div>
      <div class="emptyDesc">Try a different search term, or clear filters.</div>
      <button id="emptyClear" class="primary" type="button">Clear filters</button>
    </div>

       <!-- Groups -->
    {groups.map((g) => (
      <section class="group" id={catId(g.category)} data-group={g.category} data-expanded="false">
        <div class="groupHead">
          <div class="groupTitleWrap">
            <h2 class="groupTitle">
              {g.category} <span class="count">({g.items.length})</span>
            </h2>
            <div class="groupRule" aria-hidden="true"></div>
          </div>
        </div>

        <div class="groupReveal" data-expand-wrap>
          <div class="grid" data-expand-grid>
            {g.items.map((item) => (
<a
  class="card"
  href={`/toolkit/${item.slug}/`}
  data-title={item.data.title}
  data-category={item.data.primaryCategory || "Uncategorized"}
  data-when={item.data.whenToUse || ""}
  onclick={`trackEvent('framework_click', { framework: '${item.slug}', category: '${(item.data.primaryCategory || "Uncategorized").replace(/'/g, "\\'")}', featured: false })`}
>
                {item.data.cover ? (
                  <div class="thumb" aria-hidden="true">
                    <img class="cover" src={item.data.cover} alt={`Cover for ${item.data.title}`} loading="lazy" />
                  </div>
                ) : (
                  <div class="thumb thumbEmpty" aria-hidden="true">
                    <span class="thumbMark">AL</span>
                  </div>
                )}

                <div class="meta">
                  <div class="cardTitle">{item.data.title}</div>

                  <div class="excerpt">
                    <div class="excerptLabel">When to use</div>
                    <div class="whenToUse">
                      {item.data.whenToUse ? item.data.whenToUse : <span class="muted">—</span>}
                    </div>
                  </div>
                </div>
              </a>
            ))}
          </div>
        </div>

<div class="groupFooter">
  <button
    class="groupToggle"
    type="button"
    data-expand-toggle
    hidden
    aria-expanded="false"
    aria-label="View all frameworks in this section"
  >
<span class="groupTogglePill" aria-hidden="true">
  <img class="groupToggleIcon" src="/homepage/downarrow.png" alt="" />
</span>
  </button>
</div>
      </section>
    ))}

    <!-- Back to top -->
    <button id="toTop" class="toTop" type="button" aria-label="Back to top">↑ Top</button>

<script is:inline>
  window.addEventListener("DOMContentLoaded", () => {
    try {
      const root = document.querySelector(".toolkit");
      const q = document.getElementById("q");
      const cat = document.getElementById("cat");
      const clear = document.getElementById("clear");
      const empty = document.getElementById("empty");
      const emptyClear = document.getElementById("emptyClear");
      const resultsText = document.getElementById("resultsText");

      const viewGrid = document.getElementById("viewGrid");
      const viewCompact = document.getElementById("viewCompact");
      const toTop = document.getElementById("toTop");

      const expandableSections = Array.from(
        document.querySelectorAll("section.group, section.featuredBlock")
      );

      if (!root) throw new Error("Toolkit root not found (.toolkit).");

      function normalize(s) {
        return (s || "").toLowerCase().trim();
      }

      const controlsBar = document.querySelector(".controlsBar");
      const toolkitRoot = document.querySelector(".toolkit");

      function syncStickyControlBar() {
        if (!controlsBar) return;

        const stickyTop = parseFloat(getComputedStyle(controlsBar).top || "0") || 0;
        const rect = controlsBar.getBoundingClientRect();
        const isSticky = rect.top <= stickyTop + 0.5;

        controlsBar.classList.toggle("isStickyMobile", isSticky);

        if (isSticky && toolkitRoot) {
          const hostRect = toolkitRoot.getBoundingClientRect();
          const leftExpand = Math.max(0, hostRect.left);
          const rightExpand = Math.max(0, window.innerWidth - hostRect.right);
          controlsBar.style.setProperty("--sticky-expand-left", `${leftExpand}px`);
          controlsBar.style.setProperty("--sticky-expand-right", `${rightExpand}px`);
        } else {
          controlsBar.style.removeProperty("--sticky-expand-left");
          controlsBar.style.removeProperty("--sticky-expand-right");
        }
      }

      function queueStickyControlBarSync() {
        window.requestAnimationFrame(syncStickyControlBar);
      }

      function getHasActiveFilter() {
        return Boolean(normalize(q?.value) || (cat?.value || ""));
      }

      function setView(view) {
        root.dataset.view = view;

        const isGrid = view === "grid";
        viewGrid?.classList.toggle("isActive", isGrid);
        viewCompact?.classList.toggle("isActive", !isGrid);

        viewGrid?.setAttribute("aria-pressed", String(isGrid));
        viewCompact?.setAttribute("aria-pressed", String(!isGrid));

        try {
          localStorage.setItem("toolkitView", view);
        } catch {}

        requestAnimationFrame(() => refreshExpandableSections(getHasActiveFilter()));
      }

      function countVisibleCards() {
        return document.querySelectorAll("a.card:not(.isHidden)").length;
      }

      function getVisibleCardsInSection(section) {
        return Array.from(section.querySelectorAll("a.card")).filter(
          (card) => !card.classList.contains("isHidden")
        );
      }

      function refreshExpandableSections(forceExpand = false) {
        const isCompact = root?.dataset.view === "compact";

        expandableSections.forEach((section) => {
          const wrap = section.querySelector("[data-expand-wrap]");
          const toggle = section.querySelector("[data-expand-toggle]");
          const footer = section.querySelector(".groupFooter");

          if (!wrap || !toggle || !footer) return;

          if (section.classList.contains("isHidden")) {
            toggle.hidden = true;
            footer.classList.remove("isVisible");
            toggle.setAttribute("aria-expanded", "false");
            section.classList.remove("isCollapsible", "isCollapsed", "isExpanded");
            wrap.style.removeProperty("--collapsed-h");
            return;
          }

          const cards = getVisibleCardsInSection(section);

          if (!cards.length) {
            toggle.hidden = true;
            footer.classList.remove("isVisible");
            toggle.setAttribute("aria-expanded", "false");
            section.classList.remove("isCollapsible", "isCollapsed", "isExpanded");
            wrap.style.removeProperty("--collapsed-h");
            return;
          }

          const firstTop = cards[0].offsetTop;
          const firstRow = cards.filter((card) => Math.abs(card.offsetTop - firstTop) < 2);
          const firstRowHeight = Math.max(
            ...firstRow.map((card) => card.offsetHeight + (card.offsetTop - firstTop))
          );

          const hasMoreThanOneRow = cards.length > firstRow.length;
          const peek = window.innerWidth <= 720 ? 14 : 18;
          const collapsedHeight = firstRowHeight + peek;

          const expanded = section.dataset.expanded === "true";
          const shouldForceExpand = forceExpand || isCompact;

          if (!hasMoreThanOneRow) {
            toggle.hidden = true;
            footer.classList.remove("isVisible");
            toggle.setAttribute("aria-expanded", "false");
            section.classList.remove("isCollapsible", "isCollapsed", "isExpanded");
            wrap.style.removeProperty("--collapsed-h");
            return;
          }

          if (shouldForceExpand) {
            toggle.hidden = true;
            footer.classList.remove("isVisible");
            toggle.setAttribute("aria-expanded", "true");
            section.classList.add("isCollapsible", "isExpanded");
            section.classList.remove("isCollapsed");
            wrap.style.removeProperty("--collapsed-h");
            return;
          }

          wrap.style.setProperty("--collapsed-h", `${Math.ceil(collapsedHeight)}px`);
          section.classList.add("isCollapsible");
          section.classList.toggle("isExpanded", expanded);
          section.classList.toggle("isCollapsed", !expanded);

          toggle.hidden = false;
          footer.classList.add("isVisible");
          toggle.setAttribute("aria-expanded", String(expanded));
          toggle.setAttribute(
            "aria-label",
            expanded ? "Show less frameworks in this section" : "View all frameworks in this section"
          );
        });
      }

      function applyFilter() {
        const query = normalize(q?.value);
        const selected = cat?.value || "";

        const sections = document.querySelectorAll("section.group, section.featuredBlock, section.spotlightBlock");

        sections.forEach((section) => {
          const cards = section.querySelectorAll("a.card");
          let visibleCount = 0;

          cards.forEach((card) => {
            const title = normalize(card.dataset.title);
            const c = card.dataset.category || "";
            const when = normalize(card.dataset.when || "");

            const catText = normalize(c);
            const matchText = !query || title.includes(query) || catText.includes(query) || when.includes(query);
            const matchCat = !selected || c === selected;

            const show = matchText && matchCat;
            card.classList.toggle("isHidden", !show);
            if (show) visibleCount++;
          });

          section.classList.toggle("isHidden", visibleCount === 0);
        });

        const totalVisible = countVisibleCards();
        if (empty) empty.hidden = totalVisible !== 0;

        if (resultsText) {
          const suffix = totalVisible === 1 ? "framework" : "frameworks";
          resultsText.textContent = `Showing ${totalVisible} ${suffix}`;
        }

        refreshExpandableSections(Boolean(query || selected));
      }

      function clearFilters() {
        if (q) q.value = "";
        if (cat) cat.value = "";
        applyFilter();
        q?.focus();
      }

      function updateToTop() {
        if (!toTop) return;
        const show = window.scrollY > 420;
        toTop.classList.toggle("isVisible", show);
      }

      try {
        const saved = localStorage.getItem("toolkitView");
        if (saved === "compact" || saved === "grid") setView(saved);
      } catch {}

      viewGrid?.addEventListener("click", () => setView("grid"));
      viewCompact?.addEventListener("click", () => setView("compact"));

    window.addEventListener("scroll", queueStickyControlBarSync, { passive: true });
    window.addEventListener("resize", queueStickyControlBarSync);
    window.visualViewport?.addEventListener("resize", queueStickyControlBarSync);
    window.visualViewport?.addEventListener("scroll", queueStickyControlBarSync);

    q?.addEventListener("input", applyFilter);
     cat?.addEventListener("change", () => {
  const selected = cat.value || "";

  if (selected) {
    trackEvent("toolkit_category_select", {
      category: selected,
      page: "pm_toolkit_index",
    });
  }

  applyFilter();
});
      clear?.addEventListener("click", clearFilters);
      emptyClear?.addEventListener("click", clearFilters);

      document.querySelectorAll("[data-expand-toggle]").forEach((btn) => {
        btn.addEventListener("click", () => {
          const section = btn.closest("section.group, section.featuredBlock");
          if (!section) return;

          const expanded = section.dataset.expanded === "true";
          section.dataset.expanded = expanded ? "false" : "true";
          refreshExpandableSections(false);
        });
      });

      window.addEventListener("resize", () => {
        refreshExpandableSections(getHasActiveFilter());
      });

      window.addEventListener("scroll", updateToTop, { passive: true });
      toTop?.addEventListener("click", () => window.scrollTo({ top: 0, behavior: "smooth" }));
      updateToTop();

      queueStickyControlBarSync();
      setTimeout(queueStickyControlBarSync, 80);
      setTimeout(queueStickyControlBarSync, 220);
      window.addEventListener("load", queueStickyControlBarSync);

      applyFilter();

      requestAnimationFrame(() => refreshExpandableSections(getHasActiveFilter()));
      setTimeout(() => refreshExpandableSections(getHasActiveFilter()), 80);
      setTimeout(() => refreshExpandableSections(getHasActiveFilter()), 260);

      window.addEventListener("load", () => {
        refreshExpandableSections(getHasActiveFilter());
      });
    } catch (e) {
      console.error("[toolkit] init failed:", e);
    }
  });
</script>

<style>
  .toolkit {
    width: 100%;
    overflow-x: visible;
    padding-top: 10px;
    padding-bottom: 76px;
    box-sizing: border-box;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    --tk-radius: 18px;
    --tk-radius-sm: 12px;
  }

    .group,
  .featuredBlock,
  .spotlightBlock {
    scroll-margin-top: 102px;
  }

  .isHidden {
    display: none !important;
  }

  .controlsBar {
    position: sticky;
    top: calc(var(--siteHeaderH, 64px) + 8px);
    z-index: 40;
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
    align-items: center;
    padding: 14px 16px;
    min-height: 56px;
    width: 100%;
    box-sizing: border-box;
    border-radius: 18px;
    border: 1px solid color-mix(in srgb, var(--border) 76%, transparent);
    background:
      linear-gradient(
        180deg,
        rgb(from var(--surface) r g b / 0.72),
        rgb(from var(--surface) r g b / 0.64)
      );
    backdrop-filter: blur(14px) saturate(118%);
    -webkit-backdrop-filter: blur(14px) saturate(118%);
    box-shadow:
      0 1px 0 rgba(255,255,255,0.7) inset,
      0 16px 34px rgba(0,0,0,0.035);
    margin: 0 0 14px;
    transition:
      box-shadow 0.2s var(--ease),
      border-color 0.2s var(--ease),
      border-radius 0.22s var(--ease),
      margin 0.22s var(--ease),
      padding 0.22s var(--ease),
      width 0.22s var(--ease);
  }

  .controlsBar:focus-within {
    border-color: color-mix(in srgb, var(--border) 90%, transparent);
    box-shadow:
      0 1px 0 rgba(255,255,255,0.78) inset,
      0 20px 40px rgba(0,0,0,0.045);
  }

  .controlsBar.isStickyMobile {
    width: auto;
    max-width: none;
    margin-left: calc(var(--sticky-expand-left, 0px) * -1);
    margin-right: calc(var(--sticky-expand-right, 0px) * -1);
    border-radius: 0;
    border-left-color: transparent;
    border-right-color: transparent;
    padding-left: max(18px, env(safe-area-inset-left, 0px));
    padding-right: max(18px, env(safe-area-inset-right, 0px));
  }

  .analyticsStrip {
    margin-bottom: 18px;
  }

  .analyticsGrid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 14px;
  }

  .analyticsCard {
    position: relative;
    min-width: 0;
    padding: 18px 18px 16px;
    border-radius: 20px;
    border: 1px solid color-mix(in srgb, var(--border) 76%, transparent);
    background:
      linear-gradient(180deg, color-mix(in srgb, var(--surface) 99%, white 1%), color-mix(in srgb, var(--surface) 97%, black 3%));
    box-shadow:
      0 1px 0 rgba(255,255,255,0.74) inset,
      0 18px 32px rgba(15, 23, 42, 0.05);
  }


  .analyticsTitle {
    color: var(--ink);
    font-size: 1.02rem;
    font-weight: 650;
    letter-spacing: -0.022em;
    line-height: 1.2;
    margin-bottom: 8px;
  }

  .analyticsFrameworkName {
    color: color-mix(in srgb, var(--ink-muted) 78%, var(--ink) 22%);
    font-size: 0.9rem;
    font-weight: 500;
    letter-spacing: -0.015em;
    line-height: 1.35;
    min-height: 1.35em;
    margin-bottom: 18px;
  }

  .analyticsEventRow {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    padding-top: 14px;
    border-top: 1px solid color-mix(in srgb, var(--border) 68%, transparent);
  }

  .analyticsEventLabel {
    flex: 0 0 auto;
    padding-inline: 0;
    min-height: 0;
    border-radius: 0;
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    background: transparent;
    border: 0;
    color: color-mix(in srgb, var(--ink-muted) 82%, var(--ink) 18%);
  }

  .analyticsEventValue {
    min-width: 0;
    color: var(--ink);
    font-size: 0.82rem;
    font-weight: 600;
    letter-spacing: -0.01em;
    text-align: right;
  }

  .field {
    flex: 1;
    min-width: 260px;
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 11px 13px;
    border-radius: 13px;
    border: 1px solid color-mix(in srgb, var(--border) 76%, transparent);
    background: color-mix(in srgb, var(--surface) 99%, black 1%);
    transition: border-color 0.18s var(--ease), background 0.18s var(--ease);
  }

  .field:hover {
    border-color: color-mix(in srgb, var(--border) 88%, transparent);
  }

  .field:focus-within {
    border-color: color-mix(in srgb, var(--border) 94%, transparent);
    background: var(--surface);
  }

  .icon {
    opacity: 0.58;
    font-size: 14px;
    user-select: none;
    color: var(--ink-muted);
  }

  .input {
    width: 100%;
    border: 0;
    outline: 0;
    background: transparent;
    color: var(--ink);
    font-size: 14px;
    line-height: 1.2;
    font-family: var(--font-sans);
  }

  .input::placeholder {
    color: color-mix(in srgb, var(--ink-muted) 88%, var(--ink) 12%);
  }

.select {
  min-width: 214px;
  max-width: 248px;
  width: 100%;
  padding: 11px 40px 11px 13px;
  border-radius: 13px;
  border: 1px solid color-mix(in srgb, var(--border) 76%, transparent);
  background-color: color-mix(in srgb, var(--surface) 99%, black 1%);
  background-image: url("/homepage/downarrow.png");
  background-repeat: no-repeat;
  background-size: 12px 12px;
  background-position: right 14px center;
  color: var(--ink);
  font-size: 14px;
  font-family: var(--font-sans);
  font-weight: 500;
  cursor: pointer;
  transition: border-color 0.18s var(--ease), background-color 0.18s var(--ease);
  overflow: hidden;
  text-overflow: ellipsis;
  appearance: none;
  -webkit-appearance: none;
  -moz-appearance: none;
}

  .select:hover {
    border-color: color-mix(in srgb, var(--border) 88%, transparent);
  }

  .rightActions {
    margin-left: auto;
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .resultsInline {
    color: color-mix(in srgb, var(--ink-muted) 88%, var(--ink) 12%);
    font-size: 13px;
    font-weight: 500;
    white-space: nowrap;
    padding: 0 2px;
  }

  .ghost {
    min-height: 38px;
    padding: 0 12px;
    border-radius: 11px;
    border: 1px solid color-mix(in srgb, var(--border) 76%, transparent);
    background: color-mix(in srgb, var(--surface) 98%, black 2%);
    color: var(--ink);
    font-size: 13px;
    font-weight: 550;
    cursor: pointer;
    box-shadow: 0 1px 0 rgba(255,255,255,0.62) inset;
    transition:
      background-color 0.18s var(--ease),
      border-color 0.18s var(--ease),
      transform 0.18s var(--ease);
  }

  .ghost:hover {
    background: color-mix(in srgb, var(--surface) 95%, black 2%);
    border-color: color-mix(in srgb, var(--border) 92%, transparent);
    transform: translateY(-1px);
  }

  .viewToggle {
    display: flex;
    border: 1px solid color-mix(in srgb, var(--border) 76%, transparent);
    border-radius: 11px;
    overflow: hidden;
    background: color-mix(in srgb, var(--surface) 98%, black 2%);
    box-shadow: 0 1px 0 rgba(255,255,255,0.56) inset;
  }

  .toggleBtn {
    min-height: 38px;
    padding: 0 12px;
    border: 0;
    background: transparent;
    color: color-mix(in srgb, var(--ink-muted) 82%, var(--ink) 18%);
    font-size: 13px;
    font-weight: 550;
    cursor: pointer;
    transition: background 0.18s var(--ease), color 0.18s var(--ease);
  }

  .toggleBtn:hover {
    color: var(--ink);
  }

  .toggleBtn.isActive {
    background: var(--surface);
    color: var(--ink);
    box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--border) 70%, transparent);
  }

  .featuredBlock {
    margin-top: 10px;
    padding: 16px;
    border-radius: var(--tk-radius);
    border: 1px solid color-mix(in srgb, var(--border) 74%, transparent);
    background:
      linear-gradient(
        180deg,
        color-mix(in srgb, var(--surface) 98%, white 2%),
        color-mix(in srgb, var(--surface) 98%, black 2%)
      );
    box-shadow:
      0 1px 0 rgba(255,255,255,0.66) inset,
      0 12px 28px rgba(0,0,0,0.022);
  }

  .spotlightBlock {
    margin-top: 12px;
    margin-bottom: 18px;
  }

  .spotlightGrid {
    display: grid;
    gap: 14px;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    align-items: stretch;
  }

  .spotlightCard {
    border-radius: 18px;
    border-color: color-mix(in srgb, var(--border) 78%, transparent);
    box-shadow:
      0 1px 0 rgba(255,255,255,0.7) inset,
      0 14px 30px rgba(15, 23, 42, 0.032);
  }

  .spotlightCard .thumb {
    aspect-ratio: 16 / 10;
    padding: 5px;
  }

  .spotlightMeta {
    padding: 16px 16px 18px;
    min-height: 148px;
    gap: 10px;
  }

  .spotlightTop {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
    flex-wrap: wrap;
  }

  .spotlightKicker {
    font-size: 10px;
    line-height: 1;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: color-mix(in srgb, var(--ink-muted) 84%, var(--ink) 16%);
  }

  .spotlightCategory {
    display: inline-flex;
    align-items: center;
    min-height: 24px;
    padding: 0 10px;
    border-radius: 999px;
    border: 1px solid color-mix(in srgb, var(--border) 76%, transparent);
    background: color-mix(in srgb, var(--surface) 98%, black 2%);
    color: color-mix(in srgb, var(--ink-muted) 82%, var(--ink) 18%);
    font-size: 11px;
    font-weight: 600;
    letter-spacing: -0.01em;
    white-space: nowrap;
  }

  .spotlightCard .cardTitle {
    font-size: 1.04rem;
  }

  .group {
    margin-top: 25px;
  }

  .group + .group {
    margin-top: 34px;
  }

  .groupHead {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 14px;
    margin-bottom: 12px;
  }

  .groupTitleWrap {
    min-width: 0;
    display: flex;
    align-items: baseline;
    gap: 10px;
    flex-wrap: wrap;
  }

  .groupTitle {
    margin: 0;
    color: var(--ink);
        color:rgb(5, 35, 80);
    font-size: 1rem;
    font-weight: 650;
    letter-spacing: -0.02em;
    line-height: 1.2;
  }

  .count {
    font-weight: 500;
    color: color-mix(in srgb, var(--ink-muted) 88%, var(--ink) 12%);
    font-size: 0.9em;
  }

  .groupRule {
    display: none;
  }

  .groupReveal {
    position: relative;
    overflow: clip;
    transition: max-height 560ms cubic-bezier(0.22, 1, 0.36, 1);
    will-change: max-height;
  }

  .groupReveal::after {
    content: "";
    position: absolute;
    inset: auto 0 0 0;
    height: 96px;
    pointer-events: none;
    opacity: 0;
    transition: opacity 0.22s var(--ease);
    background: linear-gradient(
      180deg,
      rgba(247, 247, 248, 0) 0%,
      rgba(247, 247, 248, 0.74) 56%,
      rgba(247, 247, 248, 0.98) 100%
    );
  }

  .isCollapsible.isCollapsed .groupReveal {
    max-height: var(--collapsed-h);
  }

  .isCollapsible.isCollapsed .groupReveal::after {
    opacity: 1;
  }

 .groupFooter {
  display: none;
  justify-content: center;
  align-items: center;
  margin-top: -12px;
  position: relative;
  z-index: 3;
  padding-bottom: 4px;
}

.groupFooter.isVisible {
  display: flex;
}

.groupToggle {
  margin: 0 auto;
  width: 126px;
  min-height: 36px;
  padding: 0;
  border: 0;
  background: transparent;
  display: grid;
  grid-template-columns: 1fr auto 1fr;
  align-items: center;
  gap: 10px;
  cursor: pointer;
}

.groupToggle::before,
.groupToggle::after {
  content: "";
  height: 1px;
  background: linear-gradient(
    90deg,
    transparent 0%,
    color-mix(in srgb, var(--border) 72%, transparent) 50%,
    transparent 100%
  );
  opacity: 0.9;
}

.groupToggle[hidden] {
  display: none !important;
}

.groupTogglePill {
  width: 18px;
  height: 18px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  background: transparent;
  border: 0;
  box-shadow: none;
  border-radius: 0;
  padding: 0;
  filter: none;
  transform: none;
}

.groupTogglePill::before {
  display: none;
}

.groupToggle:hover .groupTogglePill {
  transform: translateY(-1px);
  background: transparent;
  border: 0;
  box-shadow: none;
  filter: none;
}

.groupToggleIcon {
  width: 14px;
  height: 14px;
  display: block;
  object-fit: contain;
  opacity: 0.98;
  transform: rotate(0deg);
  transition:
    transform 0.28s var(--ease),
    opacity 0.2s var(--ease);
}

.groupToggle:hover .groupToggleIcon {
  opacity: 1;
}

.isExpanded .groupToggleIcon {
  transform: rotate(180deg);
  opacity: 1;
}

  .grid {
    display: grid;
    column-gap: 12px;
    row-gap: 16px;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    align-items: stretch;
  }

  .card {
    display: flex;
    flex-direction: column;
    border-radius: 16px;
    text-decoration: none;
    overflow: hidden;
    border: 1px solid color-mix(in srgb, var(--border) 74%, transparent);
    background:
      linear-gradient(
        180deg,
        color-mix(in srgb, var(--surface) 99%, white 1%),
        color-mix(in srgb, var(--surface) 98%, black 2%)
      );
    box-shadow:
      0 1px 0 rgba(255,255,255,0.68) inset,
      0 10px 24px rgba(15, 23, 42, 0.026);
    height: 100%;
    color: inherit;
    transition:
      transform 0.2s var(--ease),
      box-shadow 0.2s var(--ease),
      border-color 0.2s var(--ease);
    box-sizing: border-box;
  }

  .card:hover {
    transform: translateY(-2px);
    border-color: color-mix(in srgb, var(--border) 92%, transparent);
    box-shadow:
      0 1px 0 rgba(255,255,255,0.78) inset,
      0 16px 32px rgba(15, 23, 42, 0.04);
  }

  .thumb {
    width: 100%;
    aspect-ratio: 16 / 9;
    padding: 4px;
    box-sizing: border-box;
    background:
      linear-gradient(180deg, rgba(255,255,255,0.16) 0%, rgba(255,255,255,0.02) 100%);
    display: flex;
    align-items: stretch;
    justify-content: stretch;
    position: relative;
  }

  .thumb::after {
    content: "";
    position: absolute;
    inset: 4px;
    border-radius: 12px;
    box-shadow: inset 0 0 0 1px rgba(255,255,255,0.08);
    pointer-events: none;
  }

  .cover {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    display: block;
    transition: transform 0.22s var(--ease);
    border-radius: 12px;
    background: #0b1018;
    box-shadow: none;
  }

  .card:hover .cover {
    transform: scale(1.012);
  }

  .thumbEmpty {
    background: linear-gradient(145deg, rgba(0, 0, 0, 0.025), rgba(0, 0, 0, 0.01));
  }

  .thumbMark {
    font-weight: 700;
    letter-spacing: 0.08em;
    font-size: 12px;
    color: var(--ink-muted);
    border: 1px solid color-mix(in srgb, var(--border) 76%, transparent);
    padding: 8px 12px;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.75);
    margin: auto;
  }

  .meta {
    padding: 14px 14px 16px;
    display: flex;
    flex-direction: column;
    gap: 8px;
    min-height: 124px;
    height: 100%;
    box-sizing: border-box;
  }

  .cardTitle {
    margin: 0;
    color: var(--ink);
    font-weight: 650;
    letter-spacing: -0.018em;
    line-height: 1.18;
    font-size: 1rem;
  }

  .excerpt {
    display: flex;
    flex-direction: column;
    gap: 6px;
    margin-top: 0;
    flex: 1;
  }

  .excerptLabel {
    font-size: 10px;
    line-height: 1.1;
    letter-spacing: 0.085em;
    text-transform: uppercase;
    color: color-mix(in srgb, var(--ink-muted) 84%, var(--ink) 16%);
    display: flex;
    align-items: center;
    gap: 6px;
    font-weight: 700;
  }

  .excerptLabel::before {
    content: "";
    width: 8px;
    height: 1px;
    background: color-mix(in srgb, var(--border) 80%, transparent);
  }

  .whenToUse {
    color: color-mix(in srgb, var(--ink-secondary, var(--ink-muted)) 88%, var(--ink) 12%);
    font-size: 0.875rem;
    line-height: 1.48;
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
    min-height: calc(1.48em * 3);
    max-height: calc(1.48em * 3);
  }

  .muted {
    color: var(--ink-muted);
  }

  .empty {
    margin: 22px 0 12px;
    padding: 24px 18px;
    border-radius: var(--tk-radius);
    border: 1px solid color-mix(in srgb, var(--border) 74%, transparent);
    background:
      linear-gradient(
        180deg,
        color-mix(in srgb, var(--surface) 98%, white 2%),
        color-mix(in srgb, var(--surface) 98%, black 2%)
      );
    text-align: center;
    box-shadow: 0 1px 0 rgba(255,255,255,0.6) inset;
  }

  .emptyTitle {
    font-weight: 650;
    font-size: 1rem;
    margin-bottom: 6px;
    color: var(--ink);
    letter-spacing: -0.015em;
  }

  .emptyDesc {
    color: color-mix(in srgb, var(--ink-secondary, var(--ink-muted)) 88%, var(--ink) 12%);
    margin-bottom: 14px;
    font-size: 14px;
    line-height: 1.45;
  }

  .empty .primary {
    min-height: 38px;
    padding: 0 12px;
    border-radius: 11px;
    border: 1px solid color-mix(in srgb, var(--border) 78%, transparent);
    background: color-mix(in srgb, var(--surface) 98%, black 2%);
    color: var(--ink);
    font-size: 13px;
    font-weight: 550;
    cursor: pointer;
    box-shadow: 0 1px 0 rgba(255,255,255,0.55) inset;
    transition: border-color 0.18s var(--ease), transform 0.18s var(--ease);
  }

  .empty .primary:hover {
    border-color: color-mix(in srgb, var(--border) 92%, transparent);
    transform: translateY(-1px);
  }

  .toTop {
    position: fixed;
    right: 20px;
    bottom: 20px;
    z-index: 60;
    min-height: 36px;
    padding: 0 12px;
    border-radius: 999px;
    border: 1px solid color-mix(in srgb, var(--border) 78%, transparent);
    background: color-mix(in srgb, var(--surface) 96%, white 4%);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    box-shadow:
      0 1px 0 rgba(255,255,255,0.62) inset,
      0 10px 20px rgba(0,0,0,0.05);
    cursor: pointer;
    opacity: 0;
    transform: translateY(8px);
    pointer-events: none;
    transition: opacity 0.2s var(--ease), transform 0.2s var(--ease), border-color 0.2s var(--ease);
    font-size: 12px;
    font-weight: 600;
    color: color-mix(in srgb, var(--ink-muted) 78%, var(--ink) 22%);
  }

  .toTop.isVisible {
    opacity: 0.98;
    transform: translateY(0);
    pointer-events: auto;
  }

  .toTop:hover {
    color: var(--ink);
    border-color: color-mix(in srgb, var(--border) 92%, transparent);
  }

  .ghost:active,
  .toggleBtn:active {
    transform: translateY(0);
  }

  @media (max-width: 1180px) {
    .spotlightGrid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .grid {
      grid-template-columns: repeat(4, minmax(0, 1fr));
      column-gap: 12px;
      row-gap: 16px;
    }
  }

  @media (max-width: 1040px) {
    .spotlightGrid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
      column-gap: 12px;
      row-gap: 16px;
    }
  }

  @media (max-width: 860px) {
    .grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      column-gap: 12px;
      row-gap: 16px;
    }

    .group + .group {
      margin-top: 28px;
    }
  }

  @media (max-width: 720px) {
    .spotlightGrid {
      grid-template-columns: 1fr;
      gap: 12px;
    }

    .spotlightMeta {
      min-height: 0;
    }

    .controlsBar {
      top: calc(var(--siteHeaderH, 64px) - 1px);
      padding: 12px;
      border-radius: 16px;
      gap: 8px;
      margin-bottom: 12px;
    }

    .controlsBar.isStickyMobile {
      padding-left: max(12px, env(safe-area-inset-left, 0px));
      padding-right: max(12px, env(safe-area-inset-right, 0px));
    }

    .analyticsGrid {
      grid-template-columns: 1fr;
      gap: 12px;
    }


    .field {
      min-width: 100%;
      padding: 10px 11px;
    }

    .select {
      min-width: 100%;
      max-width: none;
    }

    .rightActions {
      width: 100%;
      margin-left: 0;
      justify-content: space-between;
      gap: 8px;
    }

    .featuredBlock {
      padding: 12px;
      border-radius: 16px;
    }

    .groupHead {
      margin-bottom: 10px;
    }

    .groupFooter {
      margin-top: -9px;
      padding-bottom: 2px;
    }

    .groupToggle {
      width: 116px;
      gap: 8px;
    }

    .groupTogglePill {
      width: 38px;
      height: 28px;
    }

    .card {
      border-radius: 16px;
    }

    .meta {
      padding: 13px 13px 14px;
      gap: 8px;
      min-height: 118px;
    }

    .cardTitle {
      font-size: 0.96rem;
    }

    .whenToUse {
      font-size: 0.85rem;
      line-height: 1.46;
      min-height: calc(1.46em * 3);
      max-height: calc(1.46em * 3);
    }

    .toolkit[data-view="compact"] .thumb {
      width: 138px;
    }

    .toTop {
      right: 14px;
      bottom: 14px;
    }
  }

  @media (max-width: 640px) {
    .toolkit {
      padding-bottom: 54px;
    }

    .grid {
      grid-template-columns: 1fr;
      row-gap: 12px;
    }
  }

  .toolkit[data-view="compact"] .grid {
    grid-template-columns: 1fr;
    row-gap: 12px;
  }

  .toolkit[data-view="compact"] .card {
    flex-direction: row;
    height: auto;
  }

  .toolkit[data-view="compact"] .thumb {
    width: 188px;
    aspect-ratio: 16 / 10;
    border-right: 1px solid color-mix(in srgb, var(--border) 70%, transparent);
    flex: 0 0 auto;
  }

  .toolkit[data-view="compact"] .meta {
    min-height: unset;
    width: 100%;
  }

  .toolkit[data-view="compact"] .whenToUse {
    -webkit-line-clamp: 2;
    min-height: calc(1.48em * 2);
    max-height: calc(1.48em * 2);
  }

  @media (max-width: 520px) {
    .resultsInline {
      display: none;
    }

    .viewToggle,
    #clear {
      display: none !important;
    }

    .controlsBar {
      padding: 12px;
      top: calc(var(--siteHeaderH, 64px) - 0px);
      border-radius: 16px;
    }

    .grid {
      grid-template-columns: 1fr;
    }

    .toolkit[data-view="compact"] .card {
      flex-direction: column;
    }

    .toolkit[data-view="compact"] .thumb {
      width: 100%;
      aspect-ratio: 16 / 9;
      border-right: 0;
      border-bottom: 1px solid color-mix(in srgb, var(--border) 70%, transparent);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    * {
      scroll-behavior: auto !important;
    }

    .card,
    .cover,
    .ghost,
    .toTop,
    .groupTogglePill,
    .groupToggleIcon,
    .groupReveal {
      transition: none !important;
    }

    .card:hover,
    .ghost:hover,
    .empty .primary:hover,
    .groupToggle:hover .groupTogglePill {
      transform: none !important;
    }
  }

  @media (max-width: 720px) {
    .toolkit {
      padding-top: 0;
    }

    .controlsBar {
      top: calc(var(--siteHeaderH, 64px) - 4px);
      margin-bottom: 12px;
    }

    .controlsBar.isStickyMobile {
      padding-top: 12px;
      padding-bottom: 12px;
    }
  }

</style>

  </div>
</PageShell>
