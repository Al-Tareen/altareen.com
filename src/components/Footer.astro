<footer class="wrap">
  <div class="inner">
    <div class="muted">© {new Date().getFullYear()} Al Tareen</div>
    <div class="muted">
      <a href="/contact">Contact</a> · <a href="/socials">Socials</a>
    </div>
  </div>
</footer>

<style>
  .wrap {
    border-top: 1px solid var(--border);
    margin-top: 64px;
    background: var(--surface);
  }

  .inner {
    max-width: var(--content-width, 980px);
    margin: 0 auto;
    padding: 28px 28px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 16px;
    flex-wrap: wrap;
  }

  .muted {
    font-size: 14px;
    font-weight: 450;
    color: var(--ink-muted);
  }

  .muted a {
    text-decoration: none;
    color: inherit;
    transition: color 0.2s var(--ease);
  }

  .muted a:hover {
    color: var(--ink-secondary);
  }
</style>
